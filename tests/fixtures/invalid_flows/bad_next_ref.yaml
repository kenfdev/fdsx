name: Invalid Flow - Bad Next Reference
description: Invalid flow for testing next reference validation
start_at: start
version: '1.0'

states:
  start:
    type: task
    provider: system
    command: 'echo "test"'
    result_path: $.result
    next: nonexistent
