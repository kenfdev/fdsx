name: Invalid Flow - Mutual Exclusive Fields
description: Invalid flow for testing mutual exclusivity validation
start_at: start
version: '1.0'

states:
  start:
    type: task
    provider: claude
    model: opus
    prompt_template: 'Some prompt'
    prompt_file: 'some_file.txt'
    result_path: $.result
    end: true
